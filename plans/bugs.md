[] The padding around the buttons is quite small. "Review Deletions" is the biggest example I've found. If this is something that is a standard style, let's fix it.
[] If I restore pictures, then go to delete afterwards, it seems like delete is not working anymore.
[] We should have a "restore all" button from the "review deleted photos" screen. Rather than going one-by-one, when the user is on deleted photos, there should be a "restore all" which removes them
[] Let's make sure the maestro tests are documented appropriately, so we know how to run them.
[] Generate a simple README to describe the project.
